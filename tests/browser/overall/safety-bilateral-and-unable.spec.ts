import { test } from "@playwright/test";

test("急性踝扭伤安全停止并保留保存出口", async () => {
  test.fixme(true, "急性踝扭伤完整路径尚未接入稳定的真实控件驱动");
});

test("双侧选择优先侧、分别评估和分别复测", async () => {
  test.fixme(true, "双侧完整路径尚未接入稳定的真实控件驱动");
});

test("没有明确主诉动作时不生成伪造动作分数", async () => {
  test.fixme(true, "无明确主诉动作完整路径尚未接入稳定的真实控件驱动");
});

test("动作无法完成的不同原因分别保留并阻止误判正常", async () => {
  test.fixme(true, "动作无法完成完整路径尚未接入稳定的真实控件驱动");
});

