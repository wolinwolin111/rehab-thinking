# RehabMind 开发→测试交接：恐动追问退役 + 成果面板按钮改名（批 A）

日期：2026-09-01
基线：main @ `7d5cc7e`（outcome-slim 批已绑定 `1d44857`）
批次 SHA：本提交（代码与本档同一提交，原子到达；SHA 即本 commit hash）

## 1. 批次内容

- 退役「当时是不是也担心继续会加重？」追问（Q2）及其字段写入链（`unableFearTogether`）。
- 两处低负荷提示改认 Q1 恐惧主因（新增共享谓词 `stoppedFromFear`，workbench-support）。
- 成果面板按钮「重新确认剩余问题」→「重新确认评估答案」。
- 裁定存档：有效处理行标签维持现状（肌肉行=肌群名、控制行=动作名；治疗名只出现在处理卡与记录）。

## 2. 断言对照表（旧 → 新）

| 旧钉 | 新钉 |
| --- | --- |
| 问句「当时是不是也担心继续会加重？」出现 | 该问句**不出现**（任何原因选择后都不追问） |
| 选项「有，担心加重」「没有，只是做不了」 | 删除（字符串在 src 中不复存在） |
| `.rm-fear-together` DOM 结构 | 删除（样式清理在移动批） |
| 字段 `unableFearTogether` 写入/清除链（fef2886 语义） | UI 不再写入；`snapshot-schema` 保留该键的 yes/no 校验（旧快照兼容，勿删） |
| 按钮「重新确认剩余问题」 | 「重新确认评估答案」（:752 面板） |
| 提示触发：`unableFearTogether === "yes"` | 提示触发：`unableReason/strengthUnableReason/functionUnableReason` 任一 `=== "fear"`（谓词 `stoppedFromFear`） |
| FR-2 多层驱动（unable→疼→恐动题→填写→切换） | 收缩为：功能卡选「做不完或不敢继续」→ 主因选「担心继续会加重」→ 断言复测卡/总结页低负荷提示出现；registry FR-2 条目相应改写 |

## 3. 新场景靶子 id

无新增。FR-2 用现有页面即可驱动（评估功能卡为真实交互，无需种子）。

## 4. 不变式

- 决策流零变化：队列、处理候选、复测、训练路径一行逻辑未动；本批纯删一层不改变决策的追问。
- 含 `unableFearTogether` 的旧快照仍可保存/恢复/通过校验（字段惰性保留，无人读写）。
- 动作卡「是什么让你停下来？」三选项、力量卡「主要卡在哪里？」四选项、功能卡原因四选项本身不变。

## 5. 非阻塞范围

- `.rm-fear-together` CSS 残留 → 移动体验批清理。
- schema 中该字段的物理移除 → 推迟到旧快照自然过期后另批处理。

## 6. 已知坑

- 提示断言必须选**主因=担心继续会加重**（Q2 已不存在，"疼+同行恐惧"信号不再采集——这是裁定的预期行为，不是回归）。
- `stoppedFromFear` 覆盖三个字段名（motion=`unableReason`、strength=`strengthUnableReason`、function=`functionUnableReason`），断言时按卡型选对字段。
- 总结页提示行文案未变（「上次担心加重的动作，先从更低负荷试起」），只是触发人群变了：真恐动用户现在**第一次**能看到它。
