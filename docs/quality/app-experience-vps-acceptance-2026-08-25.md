# App 体验 VPS 发布验收（2026-08-25）

## 结论

优化方案第 10 步已经完成。当前 Node/SQLite 候选版本已发布到 VPS，canary、备份、迁移、真实 HTTP、进程目录、资源阈值和代码回滚往返均通过。该结论不替代第 11 步的用户任务验收，也不代表 Android APK 已建立。

## 发布身份

- 地址：`https://66.154.101.204/RehabMind/`；
- build ID：`local-5af6280c6f60-dirty-3f19a94ffcf7`；
- 当前目录：`/home/rehabdeploy/rehabmind/releases/20260824-180434`；
- SQLite migration：9；
- 当前保留发布：3 个。

## 发布前门禁

- 同一构建身份下的 `test:fast`、workflow、36 个定向变异、component、SQLite/API、纵向恢复、安全、迁移兼容、依赖、性能、Lint 和 4 条最小浏览器接线全部通过；
- 源码包 548 个文件，数据库、WAL/SHM、密钥、`.env`、依赖目录、构建目录和质量产物命中数为 0；
- 本地与服务器两个发布包的 SHA-256 和字节数一致。

## 发布过程发现并修复

1. 打包器最初会收录本地 SQLite、WAL 和 SHM。该包在上传前被清单扫描拦截；现已同时增加 `.gitignore` 和打包器独立拒绝规则，并建立 `APP-PACKAGE-01` 合同。
2. 服务器 3101 端口被 8 月 23 日已删除目录中的旧 canary 孤儿进程占用。第一次候选没有启动，探针误读了旧进程的 403；生产数据库和正式进程当时均未切换。现已增加端口占用拒绝、候选 PID 存活检查、独立进程组和整组清理，并建立 `APP-DEPLOY-02` 合同。
3. 旧发布环境仍有退役邀请变量。新候选只在自己的 `.env` 中清理 `PILOT_INVITE_*`，旧目录保持可回滚；当前环境已不含邀请变量。

## VPS 证据

| 项目 | 结果 |
| --- | --- |
| canary 数据库 | 由发布前备份副本迁移；integrity `ok`、外键失败 0、迁移 9 |
| canary 页面与创建边界 | 页面可用；缺少来源/同意返回 400 |
| 生产 SQLite | 发布前在线备份；向前迁移 2 项后共 9 项，健康通过 |
| PM2 | `online`、0 次异常重启、真实 cwd 与 `current` 一致 |
| HTTPS 与资源 | 页面 200，CSS/JS MIME 与内容探针通过 |
| 权限边界 | 空创建 400；匿名管理员读取 401 |
| 构建身份 | 真实 HTTP 与生成清单均为当前 build ID |
| canary 清理 | 3101 无监听，未留下孤儿进程 |
| 代码回滚往返 | 上一版启动与主页探针通过，再切回当前版通过 |
| 恢复副本 | recovery-test 备份完整性与 9 个迁移通过 |

## VPS 只读页面预检

发布后使用全新浏览器上下文读取真实 HTTPS 页面，不创建案例、不写入用户内容：

- `1440×1000` 与 `320×568`、`360×800`、`390×844`、`412×915`、`430×932` 均无整体横向溢出；
- 六个视口均显示“你的线上康复助手”，且“开始康复”位于首屏可见区域；
- 页面标题正确，控制台和页面运行时错误为 0；
- 该预检只证明部署接线和首屏布局，价值理解与流程信息密度仍由用户任务验收决定。

## 当前边界

- 第 11 步仍需由产品使用者完成一次短任务：价值理解、信息密度、保存恢复、案例编号/反馈和手机布局；
- 本次发布没有执行复杂业务浏览器穷举，业务组合继续由生产函数、工作流和 SQLite 测试证明；
- 历史 PM2 错误日志中仍保留 8 月 23 日旧版本记录，本次发布后未观察到新增错误；
- 未完成用户任务验收前，不构建 APK，也不开放粉丝群。
