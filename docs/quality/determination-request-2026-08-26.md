# 定性请求单：测试红灯与巡检发现的意图确认

生成日期：2026-08-26
生成者：测试会话
接收者：开发会话（由产品转发）
用途：以下每条请逐条回答「**有意变更 / 无意回归**」并给出一句依据。回答后由测试会话执行对应动作：有意 → 更新测试预期与正式文档；无意 → 转缺陷修复。测试侧在收到书面答复前不会自行改动任何预期。

证据链标记说明：①=引入变更的提交考古；②=正式文档/源码现状核对。

---

## RQ-1 首屏价值承诺句迁移（对应红灯：first-use-entry-contract / save-promise-copy·test1）

- **我的期望**：`rehabmind-onboarding.tsx` 必须包含「把悦舒运动康复的线下经验带到你身边，陪你完成每一次康复」（首发文案表）。
- **现状**：该句已从源码消失；新组件 `guide-cards.tsx` 出现近似句「把线下康复经验带到这里」。
- **① 提交考古**：句子消失于 `cf47910`（自述 guide cards redesign / focus tutorial restoration）。
- **② 核对**：「你的线上康复助手」「开始康复」「继续以前的康复」等其余首屏文案均在；仅此句被改写。
- **待答**：价值承诺句是否有意改为引导卡版本？旧句是否废弃？

## RQ-2 顶栏同步状态标签整合（对应红灯：save-promise-copy·test3）

- **我的期望**（SAVE-01 验收文案表）：7 个状态标签全部存在——本机保存中/已保存到本机/同步中/已同步/待处理冲突/本机保存失败/仅本机保存。
- **现状**：只剩 4 个（待处理冲突、本机保存失败、仅本机保存、网络断开正在本机保存）。新逻辑为**正常流转态完全静默，仅异常态显示**。
- **① 提交考古**：标签增删发生于 `9df84b4`→`cf47910`（自述 save status noise reduction）。
- **② 核对**：`workbench` 中 idle/local-saving/syncing/local-saved/synced 五个正常态不再渲染任何标签。
- **待答**：降噪为有意设计无疑；请确认**新的验收文案表**（哪些状态显示、显示什么），以便我更新 SAVE-01 合同与移动端 `mobileSaveStatus` 是否需要对齐口径。

## RQ-3 atlas-v2 图片引用解禁（对应红灯：muscle-region-location-picker）

- **我的期望**：picker 源码禁止出现 `rehabmind-region-*atlas-v2.png`（旧合同将其列为待淘汰素材）。
- **现状**：小腿背面视图引用 `/rehabmind-region-calf-atlas-v2.png`，且该文件真实存在于 `public/`；同批还有 ankle/foot/knee/thigh 的 v2 图。
- **① 提交考古**：引用出现于 `cf47910`（自述 body map hints）。
- **② 核对**：v1 与 v2 两套图并存于 public/。
- **待答**：v2 图是否为已验收的现行素材？若是，我将解除合同禁令并改为校验「引用的图片必须存在于 public/」；若否（仍是占位图），维持禁令并转 CONTENT-01 渠道。

## RQ-4 SSR 首屏不含引导层（原 F-1，定性方向修正）

- **现象**：dev SSR 返回的 HTML 不含「你的线上康复助手」，也不含「开始康复」按钮；这些内容在 hydration 后由客户端渲染。浏览器实际体验正常（探针可点击命中）。
- **修正说明**：此前 F-1 报告为「文案缺失」是误判——真相是**引导层整体不在 SSR 产物里**。
- **待答**：当前「SSR 输出工作台骨架、首用流程纯客户端渲染」是否为既定架构？若是，我将把巡检该项从 SSR 断言降级为客户端渲染后断言；若否（期望 SSR 直出价值页），属性能/SEO 缺陷另立编号。

## RQ-5 「问题反馈」入口首屏不可见（原 F-2，定性方向修正）

- **现象**：1440px 桌面首屏（未创建案例、引导未完成时）DOM 中存在按钮但不可见；创建案例后 release 测试证明其可见可用。
- **修正说明**：此前 F-2 判「疑似遮挡」不成立——更可能是**入口在设计上仅在工作台就绪后出现**。
- **待答**：确认设计意图。若确认，我把巡检遮挡探针的前提改为「创建匿名案例之后」再检测；若首屏也应有反馈入口，则按可达性缺陷处理。

## RQ-6 可访问性两项（axe serious/critical）

| 编号 | 违规 | 命中节点 | 待答 |
| --- | --- | --- | --- |
| RQ-6a | aria-prohibited-attr | `.rm-step-progress` div 使用了 `aria-label="第 x 步，共 y 步"` 但无 role | 加 `role="img"`+aria-label 或改用文本节点，选哪种？ |
| RQ-6b | color-contrast | 页脚免责声明「康复建议不能替代医生诊断…」对比度不足 | 提亮前景色还是加深背景色？ |

## C-1 / C-2 清理项（无需定性，直接排期）

- C-1：删除死命令词汇 `enter-training` / `remain-in-treatment`（类型定义、adapter case、orchestrator.test 中两处反向断言）。
- C-2：删除 `onboarding-steps.ts:15` 未用导入 `CSSProperties`（lint 当前唯一警告）。

---

## 回答格式建议

```
RQ-1: 有意 —— 依据：<一句话>          （→ 测试会话将更新 first-use/save-promise 预期）
RQ-2: 有意 —— 新文案表如下：…
…
C-1/C-2: 排期到 <批次>
```

回答回收后，测试会话将在一个工作日内完成全部预期更新与回归，并回报结果。
