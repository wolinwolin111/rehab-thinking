# RehabMind 知识库重构批次：开发→测试交接

> 日期：2026-08-30
> 当前开发基线：`8150b06`（buildId 由测试会话自行构建生成）
> 测试会话上次进度：**`92fc130` + `cc2ae08`（2026-08-26 晚，H batch）**
> 跨度：52 个提交 / 三个工作日。本文档不按提交叙事，按**你要修改的东西**组织。
> 工作树边界：开发未触碰测试侧任何未提交改动（docs/handover、docs/quality、tests/、scripts/quality/inspect-local.mjs、release.generated.ts 均保留 QA 原样）。

---

## 第一部分：测试资产修改清单（拿到就能动手）

### 1. 基础设施（先做，后面全依赖它）

| 资产 | 修改内容 | 原因 |
|------|---------|------|
| 全部内嵌快照样本 / fixture | **v2 → v3 四段结构**：`schemaVersion / identity / domain / workflow / draft`；identity 含 `caseId/problemThreadId/sessionId/sessionNumber/sessionStatus/capabilitySnapshotId` + `problemThreads[]`/`sessionIndex[]` | 快照 schema 已升级，旧样本恢复即失败 |
| `build-trial-targets-scenario.test.mjs` 的 bundle | 把 `knee-p0-runtime` / `ankle-p0-runtime` / `p1-runtime` / `p0-assessment-access` / `complaint-branch-matching` 拼进 bundle，或改为真实模块加载 | harness 缺模块 → 当前 4 个基线失败（开发已 stash 对照确认非回归） |
| 旧特殊检查 id 的全部引用 | 删除/替换 6 个已删除 id：`knee-mcmurray`、`knee-ligament-screen`、`knee-patellar-assist`、`knee-patella-tenderness-self`、`knee-joint-line-tenderness`、`ankle-windlass` | 生产代码已删除这些检查，相关用例整体失效 |
| `scenario-registry.json` / 覆盖矩阵 | 登记第三部分的全新场景（B/C/D 组），退役旧特殊检查场景 | 新能力无历史用例 |

### 2. 旧用例预期修改对照表（新行为是正确的，不要当 bug 报）

| # | 旧行为（旧断言） | 新正确行为 |
|---|----------------|-----------|
| 1 | 膝伸直受限 → 出现大腿前后侧等肌肉松解候选 | 普通模式无触诊时**处理队列为空 + 专业确认/补查出口**；做了统一触诊才出现对应紧张区域候选；有被动+主动证据才出合同处理单元 |
| 2 | 证据等级（P0-P3）影响评估排序 | 等级已退役；同匹配度下顺序可能与旧断言不同 |
| 3 | 问诊"诱发场景"“具体动作"分两题 | 合并为单题"**什么时候最明显？**"，只出现一次 |
| 4 | 小腿内侧主诉自动带出外翻方向 | 不再直推；背屈拆成**伸膝位/屈膝位**两张卡；外侧柱新增骰骨独立检查 |
| 5 | 多主诉共用一个分数 / 漏掉个别动作复测 | 每个实际做过的功能动作**独立复测义务** |
| 6 | 训练缺图显示"起始→发力→回位"三段式占位 | 直接从"怎么做"开始，纯文字指导，无占位 |
| 7 | 处理复测"仍受限"被进度摘要写成"范围改善" | 显示"**仍受限，未明显改变**" |
| 8 | 普通问诊编辑会新增问题线程/归档事件 | 不再膨胀；只有显式"出现新问题"才新建线程 |
| 9 | 肌肉定位图矩形色块、大腿后侧缺图 | SVG 肌群路径、四视角独立底图、小腿后侧一整块三头肌、色块不越关节 |
| 10 | 训练加重后其他动作仍可操作 | 加重即锁定其他动作反馈与进阶，处理后解锁 |
| 11 | 本机存储失败显示"网络断开" | 显示"**本机保存失败**"；网络/超时才显示"网络断开，正在本机保存" |

### 3. 全新场景（无历史用例，需新建）

#### B 组：证据流程（膝踝 P0/P1）
- **B-1** 膝伸直受限完整闭环：主动/被动都受限 → 活动范围路径处理 → 复查仅膝伸直方向；处理卡带合同单元文案。
- **B-2** 被动恢复+主动不足（专业模式）→ 末端伸膝控制路径；普通模式不出现。
- **B-3** 普通模式伸直受限+未做触诊 → 队列可能为空 + 专业确认/补查出口；**禁止**静默进训练或总结。
- **B-4** 小腿内侧不推外翻（已并入对照表 #4，验收时一并看）。
- **B-5** 权限边界：普通模式不出现被动活动/末端感觉/关节检查/专项检查卡；旧快照被动证据不参与普通模式当前判断。
- **B-6** 封存规则零出现：腘绳肌离心/腓肠神经/胫神经任何候选出现即为缺陷。

#### C 组：继续排查（全新能力，重点）
- **C-1** 完整链路：主诉"下蹲疼"→ 首轮查完/处理复查完成、分数未降 → 处理完成面板出现"XX还没有得到解释——还可以检查：…"→ 点"继续检查这些方向" → 回评估，建议项已在清单 → 补查 → 计划刷新 → 新处理/复查。
- **C-2** 跳过路径：点"进入训练"不阻断；同会话不重复弹同组建议。
- **C-3** 收敛：接受并查完所有可查项后卡片不再出现。
- **C-4** 会话隔离：第一次会话接受建议未查 → 开始第二次康复 → 新会话清单不残留、状态已清零。
- **C-5** 无固定主诉动作：不出现继续排查卡片（第一版边界，属预期）。

#### D 组：特殊检查（8 模块，全部专业模式）
- **D-1** 触发条件：急性外伤→骨折筛查；扭转/打软腿→ACL；跪地/后向不稳→PCL；内翻外翻外伤→侧副韧带；踝部对应 4 项。非对应场景不出现。
- **D-2** 安全阻断：骨折筛查任一条件满足、跟腱连续性异常/可疑 → 转影像/医学评估，普通处理不得继续。
- **D-3** 条件阻断：稳定性检查明显急性不稳 → 转医学评估；轻度 → 专业稳定性处理。
- **D-4** 普通模式 8 模块完全不可见（踝骨折问答除外，它在关键确认环节对普通用户生效）。

#### F 组：8.27 批次重跑（当时任务委派代验证，本地需重跑）
- **F-1** 双侧完整纵向闭环：左右分别评估（bilateral-assessment-ledger）→ 左右分别处理 → 左右分别复测 → 未完成侧限制训练；单侧结果不得冒充完成。
- **F-2** 训练加重锁定（同对照表 #10）。
- **F-3** 保存失败文案区分（同对照表 #11）。
- **F-4** T-09 快照陈旧：<24h 无提示；24h~7d 非阻断；≥7d 强提醒；急性 ≥7d 强制重新确认症状/安全信号/发生时间；原答案不改。
- **F-5** 多标签页冲突：双标签各自保存 → 后保存方触发冲突提示，当前页暂停写入、提供重载/保留出口。

### 4. 文档资产

- `rehabmind-test-plan.md`：纳入证据流程、继续排查、特殊检查三块新能力。
- `real-browser-coverage-matrix.md`：B/C/D/F 组新条目；旧特殊检查场景标退役。
- `regression-test-register-2026-08-26.md`：新提交区间（92fc130..8150b06）绑定。
- `scenario-registry.json`：新场景 id 登记。

---

## 第二部分：背景速览（为什么有这些变化）

一句话：**系统从"文字/标签匹配生成处理"重构为"已审核知识合同 + 评估证据链授权处理"**。膝踝处理候选必须有 `KnowledgeEvidenceLineage`（relation/branch/finding）；16 项检查权限区分普通/专业模式；特殊检查按临床指南重筛；主诉决定排查入口（结构化分支匹配替换正则）；补上了"查完没解决→继续排查"的决策闭环。

提交明细不再逐条列出，需要时查 `git log 92fc130..HEAD`。关键节点：`3d59c0f` 膝 P0 → `3415fc1` 踝 P0 → `840aa43` 权限 → `79d6736` 兜底封堵 → `f4a702e` P1 → `c55b80b` 特殊检查 → `cb466c0` 主诉分支 → `d664ece` 证据门槛 → `902dbae`/`8150b06` 继续排查。

已知取舍（owner 裁定，属预期边界）：大腿髋部及肩颈上肢冻结；跨区域继续排查建议二期；无固定主诉动作不触发继续排查；`study` 模式兼容层保留（生产入口已关闭）。

---

## 第三部分：建议回归顺序

1. `npm run test:fast`——先更新 v3 schema 断言与 bundle harness，修掉 4 个基线失败。
2. 集成/纵向：SQLite 保存恢复、事件回放、v2→v3 兼容恢复。
3. 浏览器全量（Edge full；注意第一部分对照表 #1~#11 导致的用例更新）。
4. 全新场景：B → C → D → F 组按序（C、D 无历史用例可复用）。
5. 移动预览（Pixel 5 / iPhone 13）——本批 UI 改动集中在清单/卡片/文案/定位图。
6. 结果绑定 commit `8150b06`+ / buildId / manifest / trace，回填登记表与覆盖矩阵。

## 第四部分：开发侧可判定不变式（直接当 oracle 用）

1. 膝伸直方向处理候选出现 ⇒ 必有证据链 / 触诊紧张区域 / 肿胀管理之一；否则缺陷。
2. 普通模式出现被动活动、末端感觉、关节检查、专项检查卡 ⇒ 缺陷（踝骨折关键确认问答除外）。
3. 处理完成+复查清零+主诉未解决 ⇒ 或有继续排查建议、或有明确训练出口；两者皆无为缺陷。
4. 封存 P1 规则（腘绳肌离心/腓肠神经/胫神经）出现任何运行时候选 ⇒ 缺陷。
5. 会话切换后上会话继续排查状态残留 ⇒ 缺陷。
6. 安全类特殊检查（骨折筛查/跟腱连续性）阳性后普通处理流程仍推进 ⇒ 缺陷。
7. 双侧一侧结果冒充完成、覆盖另一侧或替换优先侧 ⇒ 缺陷。

## 第五部分：明确非阻塞的未覆盖范围

- Android M2-M5（模拟器/真机/debug APK/签名包）——环境与产物缺口，不变。
- 大腿髋部、肩颈/上肢/腰骨盆——owner 裁定冻结。
- 跨区域继续排查建议（膝→踝）——二期。
- 症状收集"最近一次出现时间"、显式结构猜测字段——独立小批次未开发。
- `build-trial-targets-scenario.test.mjs` 的 4 个基线失败——harness 缺 knowledge 模块所致，更新 harness 后应转绿；若更新后仍有失败再报缺陷。
