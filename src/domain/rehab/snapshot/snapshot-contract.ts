/**
 * RehabMind v3 persistence contract revision.
 *
 * Keep this outside the React workbench so every persistence boundary validates
 * against the same value instead of duplicating a literal in UI and schema code.
 */
export const REHABMIND_V3_CONTRACT_REVISION = 3 as const;
